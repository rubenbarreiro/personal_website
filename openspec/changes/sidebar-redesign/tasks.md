## 1. Page Background

- [x] 1.1 Update `src/index.css` page background color to deep dark (`#1a1a2e`)

## 2. Sidebar Card

- [x] 2.1 Add card styles to `summary` — background color (`#16213e`), border-radius (12px), padding, margin
- [x] 2.2 Add `position: sticky; top: 20px; align-self: flex-start` to sidebar for sticky behavior on desktop

## 3. Content Card

- [x] 3.1 Add card styles to `content` — background color (`#0f3460`), border-radius (12px), padding, margin

## 4. Layout Spacing

- [x] 4.1 Add `gap: 20px` to `mainContainer` flex for spacing between cards
- [x] 4.2 Add padding to `mainContainer` so cards don't touch viewport edges

## 5. Responsive Adjustments

- [x] 5.1 Update mobile breakpoint — cards stack vertically with full width, maintain card styling
- [x] 5.2 Ensure sticky behavior is disabled on mobile (cards stack naturally)

## 6. Verification

- [x] 6.1 Run `npm run build` to verify TypeScript compilation succeeds
- [x] 6.2 Run `npm run lint` to verify no lint errors
- [x] 6.3 Visual verification at desktop, tablet, and mobile widths
