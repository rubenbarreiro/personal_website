## 1. Responsive Layout Styles

- [x] 1.1 Add mobile breakpoint (max-width: 768px) to `mainContainer` — change flex-direction to column
- [x] 1.2 Add mobile breakpoint to `summary` — set width to 100% and add padding
- [x] 1.3 Add mobile breakpoint to `content` — set width to 100% and add padding
- [x] 1.4 Add max-width constraint to `profileImage` for mobile to prevent oversized display

## 2. Global Styles Adjustment

- [x] 2.1 Update `src/index.css` body styles to allow vertical scrolling on narrow viewports (remove min-height: 100vh if it prevents scrolling)

## 3. Verification

- [x] 3.1 Run `npm run build` to verify TypeScript compilation succeeds
- [x] 3.2 Run `npm run lint` to verify no lint errors
- [x] 3.3 Verify layout visually at desktop (>1024px), tablet (769–1024px), and mobile (≤768px) widths
